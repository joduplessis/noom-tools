import React from 'react'
import { render } from '@testing-library/react'
import '@testing-library/jest-dom'

describe('Drag', () => {
    // Coming soon (experimental component / unfinished)
    // For a brief synopsis, see drag.stories.tsx

    test('render a <Draggable />', () => {
    })

    test('render a <Droppable />', () => {
    })
})